## Summary

<!-- One or two sentences: what this does and why. Which stage(s) — filter / transform / verify / build — and the motivating bug or use case. -->

## Main changes

<!--
Break the diff down by file or area, especially for anything non-trivial.
For each one: what changed and why, not just what the diff shows.

**`path/to/file.cpp`/`.hpp`** — what changed, and why
- sub-point if it's more than one piece
-->

## How was this tested?

<!-- e.g. `ctest --test-dir build`, `./tests/smoke/run_smoke_test.sh ...`, or a specific repo/file you ran the pipeline against. -->

## Checklist

- [ ] `ctest` passes
- [ ] Smoke test passes (`./tests/smoke/run_smoke_test.sh ./build/filter ./build/transform ./build/verify`), if relevant
- [ ] New/changed files have SPDX headers or are covered by `REUSE.toml`
